<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/KImage.png">
    <link rel="shortcut icon" type="image/png" href="/images/KImage.png">
    <link rel="apple-touch-icon" href="/images/KImage.png">

    <!-- CSS -->
    <link rel="stylesheet" href="/css/studentCSS/student-main.css">
    <link rel="stylesheet" href="/css/theme-toggle.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            min-height: 100vh;
        }

        .dark-theme body {
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
        }

        /* Guest Header */
        .guest-header {
            background: linear-gradient(135deg, #dc2626, #b91c1c);
            color: white;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 20px rgba(220, 38, 38, 0.3);
        }

        .guest-header-left {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .guest-header-logo {
            height: 40px;
            border-radius: 8px;
        }

        .dark-theme .guest-header-logo {
            background: white;
            padding: 4px;
            border-radius: 8px;
        }

        .guest-header-title {
            font-size: 1.25rem;
            font-weight: 600;
        }

        .guest-header-right {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .guest-user-info {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(255, 255, 255, 0.15);
            padding: 0.5rem 1rem;
            border-radius: 8px;
        }

        .guest-logout-btn {
            background: rgba(255, 255, 255, 0.15);
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 0.5rem 1rem;
            border-radius: 8px;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
        }

        .guest-logout-btn:hover {
            background: rgba(255, 255, 255, 0.25);
        }

        /* Dashboard Container */
        .dashboard-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .dashboard-welcome {
            margin-bottom: 2rem;
        }

        .dashboard-welcome h1 {
            font-size: 2rem;
            color: var(--text-color, #1e293b);
            margin-bottom: 0.5rem;
        }

        .dashboard-welcome p {
            color: var(--text-muted, #64748b);
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: var(--card-bg, #fff);
            border-radius: 16px;
            padding: 1.5rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .stat-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: white;
        }

        .stat-icon.primary {
            background: linear-gradient(135deg, #dc2626, #b91c1c);
        }

        .stat-icon.success {
            background: linear-gradient(135deg, #10b981, #059669);
        }

        .stat-icon.warning {
            background: linear-gradient(135deg, #f59e0b, #d97706);
        }

        .stat-icon.info {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
        }

        .stat-content h3 {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--text-color, #1e293b);
            margin: 0;
        }

        .stat-content p {
            font-size: 0.875rem;
            color: var(--text-muted, #64748b);
            margin: 0;
        }

        /* Section */
        .section {
            margin-bottom: 2rem;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--text-color, #1e293b);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .section-link {
            color: #dc2626;
            text-decoration: none;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .section-link:hover {
            text-decoration: underline;
        }

        /* Quiz History Table */
        .history-card {
            background: var(--card-bg, #fff);
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            overflow: hidden;
        }

        .history-table {
            width: 100%;
            border-collapse: collapse;
        }

        .history-table th,
        .history-table td {
            padding: 1rem 1.5rem;
            text-align: left;
        }

        .history-table th {
            background: var(--secondary-bg, #f8fafc);
            color: var(--text-muted, #64748b);
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            border-bottom: 1px solid var(--border-color, #e2e8f0);
        }

        .history-table td {
            border-bottom: 1px solid var(--border-color, #e2e8f0);
            color: var(--text-color, #1e293b);
        }

        .history-table tr:last-child td {
            border-bottom: none;
        }

        .history-table tr:hover td {
            background: var(--secondary-bg, #f8fafc);
        }

        .quiz-name {
            font-weight: 600;
        }

        .quiz-date {
            font-size: 0.875rem;
            color: var(--text-muted, #64748b);
        }

        .score-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            padding: 0.375rem 0.75rem;
            border-radius: 20px;
            font-weight: 600;
            font-size: 0.875rem;
        }

        .score-badge.passed {
            background: #dcfce7;
            color: #166534;
        }

        .score-badge.failed {
            background: #fee2e2;
            color: #991b1b;
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
        }

        .status-badge.completed {
            background: #dbeafe;
            color: #1e40af;
        }

        .status-badge.in-progress {
            background: #fef3c7;
            color: #92400e;
        }

        .view-link {
            color: #dc2626;
            text-decoration: none;
            font-weight: 500;
        }

        .view-link:hover {
            text-decoration: underline;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            background: var(--card-bg, #fff);
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        }

        .empty-state-icon {
            width: 80px;
            height: 80px;
            background: var(--secondary-bg, #f8fafc);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
        }

        .empty-state-icon i {
            font-size: 2.5rem;
            color: var(--text-muted, #64748b);
        }

        .empty-state h3 {
            font-size: 1.25rem;
            color: var(--text-color, #1e293b);
            margin-bottom: 0.5rem;
        }

        .empty-state p {
            color: var(--text-muted, #64748b);
            margin-bottom: 1.5rem;
        }

        .empty-state-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.875rem 1.5rem;
            background: linear-gradient(135deg, #dc2626, #b91c1c);
            color: white;
            border-radius: 10px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .empty-state-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(220, 38, 38, 0.3);
        }

        /* Available Quizzes */
        .quiz-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 1.5rem;
        }

        .quiz-card {
            background: var(--card-bg, #fff);
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .quiz-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
        }

        .quiz-card-header {
            padding: 1.5rem;
            background: linear-gradient(135deg, #dc2626, #b91c1c);
            color: white;
        }

        .quiz-card-header h3 {
            font-size: 1.125rem;
            font-weight: 600;
            margin: 0 0 0.5rem 0;
        }

        .quiz-card-code {
            font-size: 0.85rem;
            opacity: 0.9;
        }

        .quiz-card-body {
            padding: 1.5rem;
        }

        .quiz-card-meta {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .quiz-card-meta-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
            color: var(--text-muted, #64748b);
        }

        .quiz-card-meta-item i {
            color: #dc2626;
        }

        .quiz-card-btn {
            width: 100%;
            padding: 0.875rem;
            background: linear-gradient(135deg, #dc2626, #b91c1c);
            color: white;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
        }

        .quiz-card-btn:hover {
            opacity: 0.9;
        }

        /* Profile Card */
        .profile-card {
            background: var(--card-bg, #fff);
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            padding: 2rem;
        }

        .profile-header {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            margin-bottom: 1.5rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid var(--border-color, #e2e8f0);
        }

        .profile-avatar {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #dc2626, #b91c1c);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 2rem;
            font-weight: 700;
        }

        .profile-info h2 {
            font-size: 1.5rem;
            color: var(--text-color, #1e293b);
            margin: 0 0 0.25rem 0;
        }

        .profile-info .guest-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            padding: 0.25rem 0.75rem;
            background: #fef3c7;
            color: #92400e;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
        }

        .profile-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
        }

        .profile-detail-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .profile-detail-icon {
            width: 40px;
            height: 40px;
            background: var(--secondary-bg, #f8fafc);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #dc2626;
        }

        .profile-detail-content label {
            font-size: 0.75rem;
            color: var(--text-muted, #64748b);
            text-transform: uppercase;
        }

        .profile-detail-content p {
            margin: 0;
            font-weight: 500;
            color: var(--text-color, #1e293b);
        }

        @media (max-width: 768px) {
            .dashboard-container {
                padding: 1rem;
            }

            .guest-header {
                padding: 1rem;
                flex-direction: column;
                gap: 1rem;
            }

            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .history-table {
                display: block;
                overflow-x: auto;
            }

            .profile-header {
                flex-direction: column;
                text-align: center;
            }
        }
    </style>
</head>
<body class="<%= theme %>-theme">
    <!-- Guest Header -->
    <header class="guest-header">
        <div class="guest-header-left">
            <a href="/">
                <img src="/images/K.png" alt="ELKABLY" class="guest-header-logo">
            </a>
            <span class="guest-header-title">ELKABLY - Guest Dashboard</span>
        </div>
        <div class="guest-header-right">
            <div class="guest-user-info">
                <i class="fas fa-user"></i>
                <span><%= guest.fullName %></span>
            </div>
            <a href="/guest/logout" class="guest-logout-btn">
                <i class="fas fa-sign-out-alt"></i>
                <span>Exit</span>
            </a>
        </div>
    </header>

    <div class="dashboard-container">
        <!-- Welcome -->
        <div class="dashboard-welcome">
            <h1>Welcome, <%= guest.fullName %>!</h1>
            <p>Here's your guest activity overview</p>
        </div>

        <!-- Stats Grid -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon primary">
                    <i class="fas fa-file-alt"></i>
                </div>
                <div class="stat-content">
                    <h3><%= stats.totalAttempts %></h3>
                    <p>Total Attempts</p>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon success">
                    <i class="fas fa-check-circle"></i>
                </div>
                <div class="stat-content">
                    <h3><%= stats.passedAttempts %></h3>
                    <p>Passed</p>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon warning">
                    <i class="fas fa-chart-line"></i>
                </div>
                <div class="stat-content">
                    <h3><%= stats.averageScore %>%</h3>
                    <p>Average Score</p>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon info">
                    <i class="fas fa-star"></i>
                </div>
                <div class="stat-content">
                    <h3><%= stats.bestScore %>%</h3>
                    <p>Best Score</p>
                </div>
            </div>
        </div>

        <!-- Profile Card -->
        <div class="section">
            <div class="profile-card">
                <div class="profile-header">
                    <div class="profile-avatar">
                        <%= guest.fullName.charAt(0).toUpperCase() %>
                    </div>
                    <div class="profile-info">
                        <h2><%= guest.fullName %></h2>
                        <span class="guest-badge">
                            <i class="fas fa-user-clock"></i>
                            Guest User
                        </span>
                    </div>
                </div>
                <div class="profile-details">
                    <div class="profile-detail-item">
                        <div class="profile-detail-icon">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <div class="profile-detail-content">
                            <label>Email</label>
                            <p><%= guest.email %></p>
                        </div>
                    </div>
                    <div class="profile-detail-item">
                        <div class="profile-detail-icon">
                            <i class="fas fa-phone"></i>
                        </div>
                        <div class="profile-detail-content">
                            <label>Phone</label>
                            <p>+<%= guest.phoneCountryCode %> <%= guest.phone %></p>
                        </div>
                    </div>
                    <div class="profile-detail-item">
                        <div class="profile-detail-icon">
                            <i class="fas fa-users"></i>
                        </div>
                        <div class="profile-detail-content">
                            <label>Parent Phone</label>
                            <p>+<%= guest.parentPhoneCountryCode %> <%= guest.parentPhone %></p>
                        </div>
                    </div>
                    <div class="profile-detail-item">
                        <div class="profile-detail-icon">
                            <i class="fas fa-calendar"></i>
                        </div>
                        <div class="profile-detail-content">
                            <label>Joined</label>
                            <p><%= new Date(guest.createdAt).toLocaleDateString() %></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quiz History -->
        <div class="section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-history"></i>
                    Quiz History
                </h2>
            </div>

            <% if (quizHistory && quizHistory.length > 0) { %>
            <div class="history-card">
                <table class="history-table">
                    <thead>
                        <tr>
                            <th>Quiz</th>
                            <th>Attempt</th>
                            <th>Score</th>
                            <th>Status</th>
                            <th>Date</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% quizHistory.forEach(item => { %>
                        <tr>
                            <td>
                                <div class="quiz-name"><%= item.quizTitle %></div>
                            </td>
                            <td>#<%= item.attemptNumber %></td>
                            <td>
                                <span class="score-badge <%= item.passed ? 'passed' : 'failed' %>">
                                    <i class="fas <%= item.passed ? 'fa-check' : 'fa-times' %>"></i>
                                    <%= item.score %>%
                                </span>
                            </td>
                            <td>
                                <span class="status-badge <%= item.status %>">
                                    <i class="fas <%= item.status === 'completed' ? 'fa-check-circle' : 'fa-clock' %>"></i>
                                    <%= item.status === 'completed' ? 'Completed' : 'In Progress' %>
                                </span>
                            </td>
                            <td class="quiz-date">
                                <%= item.completedAt ? new Date(item.completedAt).toLocaleDateString() : 'N/A' %>
                            </td>
                            <td>
                                <% if (item.status === 'completed') { %>
                                <a href="/guest/quiz/<%= item.quizId %>/results?attempt=<%= item.attemptNumber %>" class="view-link">
                                    <i class="fas fa-eye"></i> View
                                </a>
                                <% } else { %>
                                <a href="/guest/quiz/<%= item.quizId %>/take" class="view-link">
                                    <i class="fas fa-play"></i> Resume
                                </a>
                                <% } %>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
            <% } else { %>
            <div class="empty-state">
                <div class="empty-state-icon">
                    <i class="fas fa-clipboard-list"></i>
                </div>
                <h3>No Quiz Attempts Yet</h3>
                <p>You haven't taken any quizzes yet. Start exploring and test your knowledge!</p>
                <a href="/" class="empty-state-btn">
                    <i class="fas fa-search"></i>
                    Browse Quizzes
                </a>
            </div>
            <% } %>
        </div>

        <!-- CTA -->
        <div class="section" style="text-align: center;">
            <p style="color: var(--text-muted, #64748b); margin-bottom: 1rem;">
                Want to save your progress and access more features?
            </p>
            <a href="/auth/register" class="empty-state-btn">
                <i class="fas fa-user-plus"></i>
                Create an Account
            </a>
        </div>
    </div>
</body>
</html>
